<Window x:Class="ChatClient.EmojiPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="😊 Виберіть смайлик" 
        Height="450" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E293B">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="😊 Виберіть смайлик" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <!-- ScrollViewer для прокрутки -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="0,0,0,5">

            <ItemsControl x:Name="EmojiGrid">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding}" 
                                FontSize="32"
                                Width="65" 
                                Height="65"
                                Margin="5"
                                Background="#374151"
                                Foreground="White"
                                BorderThickness="0"
                                Click="EmojiButton_Click"
                                Cursor="Hand"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" 
                Content="Скасувати" 
                Width="100" 
                Height="40"
                Margin="0,10,0,0"
                HorizontalAlignment="Center"
                Background="#EF4444"
                Foreground="White"
                BorderThickness="0"
                FontSize="14"
                FontWeight="Bold"
                Click="CancelButton_Click"/>
    </Grid>
</Window>